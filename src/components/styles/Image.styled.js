import styled from "styled-components";

export const Image = styled.img`
  display: block;
  max-width: 100%;
`

export const ImageCaption = styled.p`
  bottom: .75rem;
  font-size: .75rem;
  font-weight:600;
  position: absolute;
  right: 2rem;
`